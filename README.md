# employee-managment-assignment

_**Run the Rest backend**_:

`cd backend`

`mvn package && java -jar target/employee-management-assignment-1.0-SNAPSHOT.jar`

_**Swagger**_:

`http://localhost:8080/swagger-ui.html`